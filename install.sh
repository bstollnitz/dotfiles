#!/bin/bash

pwd > ~/whereami.txt

# script_name=$0
# dotfiles_dir=$(dirname "$0")
# rm -rf ~/.zshrc
# rm -rf ~/.dircolors
# ln -s $dotfiles_dir/.zshrc ~/.zshrc
# ln -s $dotfiles_dir/.dircolors ~/.dircolors

